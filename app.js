const startGame = () => {};

const main = () => {
  // Init data
  const grid = {
    id: "",
    state: "",
    duration: "",
  };
  let playerName = "";
  let timer = 60;
  let score = 0;
};

$(main);
